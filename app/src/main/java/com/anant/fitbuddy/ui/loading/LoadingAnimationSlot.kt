package com.anant.fitbuddy.ui.loading

/** Where a loading animation may be shown. */
enum class LoadingAnimationSlot {
    /** Wide dashboard banner while AI analyses a photo or text log. */
    ANALYZING,

    /** Compact fill inside Generate insight / Recommend with AI buttons. */
    INSIGHT,
}
